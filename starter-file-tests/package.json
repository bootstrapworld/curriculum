{
  "name": "bootstrap-starter-file-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Run every Pyret starter file the curriculum links to through a headless browser against a deployed Pyret editor.",
  "scripts": {
    "test": "node run.js",
    "list": "node run.js --list"
  },
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
