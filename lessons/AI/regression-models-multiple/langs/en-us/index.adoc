[.beta]
= Multiple Regression Models
++++
<style>
.big-codeblock .big { background: #f7f7f8; display: inline-block; padding: 15px; }
</style>
++++

@description{Students extend single-variable regression to multiple regression, discovering how a line-of-best-fit in 2D becomes a plane-of-best-fit in 3D. Using `curve` and `skew` as two explanatory variables, they build a stronger steering predictor, compare model fits, and think critically about which variables matter most.}

@lesson-prereqs{regression-models-simple}

@keywords{}

[@lesson-intro-table]
|===
| Lesson Goals
| Students will be able to...
@objectives

| Student-facing Lesson Goals
|
- Let's use two variables at once to build an even better steering predictor!
- Let's explore what a "plane of best fit" looks like in three dimensions!

| Materials
|[.materials-links]
@material-links

| Supplemental Materials
|[.materials-links]
@opt-material-links
|===

== Multiple Regression
@objective{ai-high-dimensions-regression}
@objective{ai-math-connection-regression}
=== Overview
Students extend their understanding of single-variable linear regression to multiple regression with two predictors. Using `curve` and `skew`, they see that each scatter plot from the previous lesson was a 2D projection of higher-dimensional data, and that a plane of best fit is the natural extension of a line of best fit.

=== Launch
@slidebreak{LaunchR}

@QandA{
@right{@image{images/projection.webp, 350}}
Take a look at this picture of a cylinder floating in a box.

@Q{What is making the square shadow on the blue wall?}
@A{A light shined at the side of the cylinder casts a square shadow.}

@Q{What is making the circle shadow on the yellow wall?}
@A{A *different* light shined at one end of the cylinder casts a circular shadow.}

@Q{What shape would the shadow be on the floor?}
@A{A square.}

@Q{How can one shape cast such different shadows?}
@A{A 3D object has multiple sides, so different lights cast different shadows.}

@Q{If we only saw the circle shadow, would we know for sure that it was cast by a cylinder like this?}
@A{No. It could be cast by a short or tall cylinder... or even a sphere, a cone, an hour glass, etc.}

@Q{If the _real_ shape is a cylinder, are the circle and square shadows "lying" to us?}
@A{No — they're each just telling a *part* of the story!}
}

@slidebreak{LaunchR}
@right{@image{images/steering-v-curve-lr.png, 300}}

In @lesson-link{regression-models-simple}, we made four scatter plots — one for each candidate explanatory variable. Each showed a 2D relationship between one variable and `steering-angle`.

But `curve`, `skew`, `offset`, and `speed` are all being measured _at the same time_, for the same data point. Each scatter plot we made was showing just one "shadow" of the data — a 2D projection of information that actually exists in more dimensions.
@QandA{
@Q{If `curve` and `skew` are both inputs, and `steering-angle` is the output, how many dimensions would we need to represent all three at once?}
@A{Three! One axis for `curve`, one for `skew`, one for `steering-angle`.}
}

@teacher{Give students a chance to discuss and brainstorm before moving on.}


=== Investigate
@slidebreak{Investigate-DN}

@lesson-instruction{
Let's explore what a 3-D scatter plot looks like, using a _small subset_ of our driving data:

- Complete @printable-exercise{predicting-steering-from-2.adoc} using @starter-file{driving-desmos}.
- Be ready to share your thinking!
}

@define{perspective-note}{
Our brains naturally process the 3D world using perspective! Train tracks going off into the distance will _appear_ to get closer and closer, even though they are parallel lines. Desmos lets you choose how much _perspective_ to use when drawing the plot, by clicking on the wrench icon adjusting the slider. Teachers should pay careful attention to this slider!

- *Minimum perspective* - When looking "head on" at the X-Z or Y-Z plane, we _don't_ want perspective! We want the scatter plots to resemble a flat sheet of paper as much as possible, for the plane-of-best-fit to look like a line, and for the 3D points to line up perfectly with the 3D scatter plots. Minimum perspective is what you want, when building the bridge from 2D to 3D!

- *Maximum perspective* - When rotating and viewing the cube in 3 dimensions, we _do_ want perspective! Since our brains expect to see perspective when viewing 3D space, rotating the cube without perspective makes it harder for students to engage their visual processing to make sense of things.

We've set the Desmos activity to start with minimum perspective, but teachers should be ready to adjust the perspective if students struggle to make sense of the plot when viewing it in 3D.
}

@strategy{Using Perspective in Desmos3D}{@perspective-note}

@pd-slide{
@slidebreak{Launch}
@perspective-note
}


@slidebreak{LaunchR}
@right{@image{images/plane-of-best-fit.png, 300}}

--
If we want to consider all three variables in our model, then we need to consider each snapshot in the training corpus as a 3d point, with values for `curve`, `skew`, _and_ `steering-angle`.

- If `curve` and `skew` are both values in a given snapshot, they each explain a _portion_ of the steering angle at that moment.

- To make _predictions_ in three dimensions, we need to do _regression_ in three dimensions. 

@lesson-point{
Instead of a line of best fit, we're looking for a *plane of best fit*.
}
--

@teacher{Draw connections to the cylinder-and-shadows image: each 2D scatter plot from the last lesson is a literal "shadow" of the 3D data, cast by collapsing one dimension flat.}

@slidebreak{Synthesize}

Different fields use different vocabulary for the same ideas:

[cols="^1a,^1a", options="header", stripes="none"]
|===
| In Math... | In Machine Learning...
| Variable   | Parameter
| Slope      | Weight
|===

In math, the "amount of pull" a variable has on the output is its _slope_. In machine learning, the same idea is called a _weight_. When tech companies talk about "releasing model weights" — or refuse to — they mean the learned coefficients of their regression models.

@teacher{
Some tech companies make a show of open-sourcing their models while declining to share the weights. That's a little like saying "we predict steering angle from curve and skew" but refusing to tell anyone the actual slope for either one.
}


@scrub{
@slidebreak{LaunchR}

@ifslide{@right{@image{images/plane-of-best-fit.png, 300}}}


- We need a function that takes in both slopes (@math{m_{curve}} and @math{m_{skew}}) - considering the "amount of pull" each variable has on the steering angle
- @math{b} is the intercept: the predicted steering angle when both inputs are zero
We want Pyret to learn a function with the best possible values for @math{m_{curve}}, @math{m_{skew}}, and @math{b}:

[.big-codeblock]
@big{
`fun curve-skew-predictor(r): (` @math{m_{curve}}` * r["curve"]) + ( `@math{m_{skew}} `* r["skew"]) + b end`
}
}

=== Synthesize
@slidebreak{SynthesizeR}
@ifslide{@right{@image{images/plane-of-best-fit.png, 300}}}

@QandA{
@Q{Why does our multiple-regression function have *two* slopes?}
@A{Because we're defining a *plane*. A flat sheet can tip along two different axes independently, so it needs two slopes — one for each.}

@Q{Each scatter plot from the last lesson was a "shadow" of the 3D data. When we looked at `curve` vs. `steering-angle`, in which direction was the light shining?}
@A{Along the `skew` axis — all the variation in `skew` got collapsed into a flat 2D view.}

@Q{Does that mean the `curve` vs. `steering-angle` scatter plot was "wrong"?}
@A{No — it was accurate, just incomplete. It told part of the story.}

@Q{Do we know that the model we fit in Desmos is really the best for the data?}
@A{No! We just used our eyes to find a plane that looked like it might represent the pattern in the points.}
}

== Multiple Regression in Pyret
@objective{models-multivariable}

=== Overview
Students use `regression-model-code` with multiple input columns to build a two-variable steering predictor, and `regression-model-S` to evaluate and compare models. They think critically about which variables to keep.

=== Launch
@slidebreak{Launch}

Let's use Pyret to find the best model for our data! 

@QandA{
@Q{For linear regression we used a function called `lr-plot`. What did it do?}
@A{It graphed a scatter plot with a line of best fit on it.}
@A{It defined the predictor function.}
@A{It told us how much error to expect in predictions made with the model by reporting the @math{S-value} and @math{r^2}.}
}

Pyret can't plot data in more than two dimensions at a time, but it can _compute_ in as many dimensions as we want!

- To get the code for a predictor function we can use `regression-model-code`. +
@hspace{2em}@show{(contract "regression-model-code" '((table-name Table) (explanatory List<String>) (response String)) "Code")} +
  * In the last lesson we gave it a list with just one explanatory variable. For example: +
@hspace{2em}`regression-model-code(training-data, [list: "curve"], "steering-angle")`
  * Adding a second variable to the list is all it takes to go from a regression _line_ to a *regression __plane!__* For example: +
@hspace{2em}`regression-model-code(training-data, [list: "curve", "skew"], "steering-angle")`
- Pyret also has a tool for measuring the fit of any row-consuming regression model:
@hspace{2em}@show{(contract "regression-model-S" '((table-name Table) (explanatory List<String>) (response String) (model "Row->Number")) "Number" 
"produces the S-value of a multiple-regression model, fit to the specific columns"
)}

@teacher{
Consider the trade-off between clarity and amount-of-typing: for some students, rename their predictors using a shorthand is a big win! For example, `curve-skew-offset-predictor` could be renamed `cso-pred`.
}

@slidebreak{Launch}
@lesson-point{`regression-model-S` needs to know about your explanatory columns!}

When we were calculating @math{S} in a 2-dimensional model (1 explanatory, 1 response), we needed _more than two rows_. Two points are enough to define a line, but if there's no third row then there's nothing else to fit the line _to!_ But now we're dealing with models in 3-dimensions (2 explanatory, 1 response), and we need _more than three rows_.

(That's why the list of explanatory columns matters!)


=== Investigate
@slidebreak{InvestigateR-DN}

@lesson-instruction{
@right{@image{images/self-driving-car.jpg, 300}}
- Open your copy of @starter-file{self-driving} and click "Run".
- Use `regression-model-code` and `regression-model-S` to complete the first section of @printable-exercise{supervised-learning-multiple-regression.adoc}.
}

@slidebreak{InvestigateR}
@QandA{
@ifslide{@right{@image{images/self-driving-car.jpg, 300}}}

@Q{How did the S-value change when you added `skew` as a second predictor?}
@A{It decreased — using two predictors explains more of the variation in `steering-angle`, leaving less unexplained error.}

@Q{Looking at the coefficients for `curve` and `skew`, which has more _influence_ on the predicted steering angle?}
@A{`curve` — its coefficient is larger, meaning a one-unit change in `curve` moves the prediction more than a one-unit change in `skew`.}
}

@slidebreak{Investigate}

*Sensors cost money!* Adding all the various sensors to make a car self-driving adds thousands of dollars to the cost of manufacturing, and the extra computing power needed to process the data makes the chips used in those cars more expensive too. To make cars affordable, companies need to make tough decisions about which sensors to keep and which to drop.

@lesson-instruction{
- Complete the rest of @printable-exercise{supervised-learning-multiple-regression.adoc}.
- @opt{For a deeper exploration, complete @opt-printable-exercise{dropping-variables.adoc}}
}


@slidebreak{InvestigateR}
@QandA{
@ifslide{@right{@image{images/self-driving-car.jpg, 300}}}
@Q{If you had to drop one of the two variables, which would you drop and why?}
@A{`skew` — it has the smaller coefficient and removing it would cost less accuracy than removing `curve`.}

@Q{If you couldn't use `curve` at all, could you still build a workable model?}
@A{You could build a model, but it would perform significantly worse.}
@A{`curve` carries the most information about how to steer; the other variables only fill in part of the gap.}
}

@lesson-instruction{
- Complete @printable-exercise{self-driving-car-reflection.adoc}.
}

@slidebreak{Investigate}

@lesson-point{
Multiple regression is the same idea as linear regression, extended to any number of input variables. Each input gets its own slope (weight), and the model learns all of them at once to minimize error.
}

Multiple regression is the engine behind many real AI systems:

- ALVINN's hundreds of pixel weights
- Car insurance rates based on age, location, and driving history
- Weather forecasts based on temperature, humidity, pressure, and wind speed

The same three phases always apply when training and using a model: 

. Collect labeled data.
. Train a function.
. Apply the function to new inputs.


=== Synthesize
@slidebreak{Synthesize}

@QandA{
@Q{ALVINN used hundreds of pixel values _and_ an obstacle distance as inputs. How does that connect to what we just did?}
@A{ALVINN's model had hundreds of parameters — one per pixel, each with its own learned weight — plus the obstacle distance. The same multiple regression principle applies, just at a much larger scale.}

@Q{What would it mean for a pixel to have a very _small_ weight in ALVINN's model?}
@A{That pixel barely affects the steering prediction — it probably captures a part of the image that rarely matters for navigating the road, like the sky.}

@Q{What would it mean for a pixel to have a very _large_ weight?}
@A{That pixel strongly influences the predicted steering angle — likely a part of the image that reliably signals road direction.}
}