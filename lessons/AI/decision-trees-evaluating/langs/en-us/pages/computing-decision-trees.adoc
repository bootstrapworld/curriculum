= Computing Decision Trees
++++
<style>
#preamble_disabled { flex-grow: 0 !important; }
.fitb { min-width: 2em !important; }
.autonum, .fitb.stretch::after { padding-top: 1em !important; }
table.fit-content tr td { padding: 0.3em 0.625em !important; }
#body table td:last-child img {
    height: 180px; /* we have a limit to vertical space */
	width:  250px; /* stretch the image outm slightly breaking proportion*/
}

.listingblock pre { padding: 0 !important; }
</style>
++++

@center{@show{(contract "build-tree" '("Table" "List" "String" "Number") "DecisionTree")}}

The `build-tree` function takes *four arguments* in its Domain:

[cols="1a,2a", frame="none", grid="none"]
|===
|
(a) The _training data_ on which to fit the tree +
(b) A list of all the column names to split on

|
(c) The name of the _target_ column, containing all the "answers" for the various categories +
(d) The _maximum depth_ the tree is allowed to be
|===

== Choosing the Right Split

@n In the *Definitions Area*, define a simple, 1-level tree using only the `"tail"` column, defining it as `c1`:

@indented{`tree1 = build-tree(training, [list: "tail"], "species", 1)`}

@n Click "Run", and evaluate `tree1`. What species will this classifier predict for animals *without* tails?  @fitb{}{@ifsoln{snail}}

@n Evaluate `dt-code(tree1)`. Have you seen code like this before? @fitb{}{@ifsoln{it's the code for `tail-classifier`!}}

@n Which column will make the best @vocab{root node} for predicting `species` in the `training` dataset? @fitb{}{@ifsoln{`swims`}}

@n Let's see if Pyret agrees, by letting _it_ choose from all the columns. Add this definition to your program:

@indented{`tree2 = build-tree(training, [list: "tail", "mammal", "swims"], "species", 1)`}

@vspace{1ex}

*Use the `classify` function on `tail-classifier` and `tree2`:*

@indented{
```
classify(training, "species", tail-classifier)
classify(training, "species", tree1)
classify(training, "species", tree2)
```
}

@n How many did `tail-classifer` classifier get right? @fitb{7em}{@ifsoln{7}} @hspace{2em} `tree1`? @fitb{7em}{@ifsoln{7}} @hspace{2em} `tree2`? @fitb{7em}{@ifsoln{8}}

== Deeper Trees vs. More Columns

@n Let's allow Pyret to split on use more columns, and go a lot deeper!

@indented{`tree3 = build-tree(training, [list: "tail", "mammal", "swims"], "species", 3)`}

@n How many animals does `tree3` get right? @fitb{3em}{@ifsoln{12}} Why did Shadestar get classified as a dog? @fitb{}{} +
@fitb{}{@ifsoln{Since she swims, all three column values "looks" the same as the dogs}}

@n Why did Charlotte and Webs get classified as snails? @fitb{}{} +
@fitb{}{@ifsoln{Their values for those three columns are all the same as snails, so there's no way to tell the difference!}}

@n Define `tree4` to use the *same columns* as `tree3`, but raise the max-depth to 10. Why do `tree3` and `tree4` look identical? @fitb{}{@ifsoln{There's no more information to be used, so there's no point in splitting further}}

@n Build `tree5`, this time adding the `pounds` column. How many levels deep will Pyret's tree go? @fitb{}{3}

@n Build `tree6`, using _only_ the `pounds` column and letting it go as deep as possible. How many does `tree6` get right? @fitb{}{@ifsoln{15}}

== Using our Classifiers with a Testing Dataset

@n Use the `classify` function to compare trees 4-6, this time using the `testing` dataset:

@indented{
```
classify(testing, "species", tree4)
classify(testing, "species", tree5)
classify(testing, "species", tree6)
```
}

@n Write your observations, and an explanation what you saw. @fitb{}{} +
@blanklines{2}{@ifsoln{`tree6` did horribly! It's fit specifically to the `pounds` column of the `training` dataset, which has a really different distribution in the testing dataset}}