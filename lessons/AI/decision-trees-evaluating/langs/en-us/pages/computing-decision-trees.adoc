= Computing Decision Trees
++++
<style>
#preamble_disabled { flex-grow: 0 !important; }
#body table { margin: 0 !important; }
#body table td:last-child img {
    height: 180px; /* we have a limit to vertical space */
	width: 250px;  /* stretch the image outm slightly breaking proportion*/
}
</style>
++++

[cols=".^12a, ^8a", frame="none"]
|===
|
We've provided a function that will use this algorithm to build the optimal decision tree of _any length_, using _any columns_! Here's the contract:

@vspace{1ex}

@show{(contract "build-tree" '("Table" "List" "String" "Number") "DecisionTree")}

@vspace{1ex}

The `build-tree` function takes *four arguments* in its Domain:

- The _training dataset_ from which to fit the tree
- A list of all the column names to split on
- The name of the _label_ column containing all the "answers" for the various categories
- The _maximum depth_ the tree is allowed to be

|
@image{../images/empty-tree.png}
|===


== The Optimal 1-Level Tree for this Training Data
Which column makes a better root for predicting `species` in this training dataset: @fitb{5em}{`"mammal"`}, or @fitb{5em}{`"tail"`}? Ask Pyret to build a 1-level tree while considering only those two columns: `c1 = build-tree(simple-animals, [list: "mammal", "tail"], "species", 1)`

@n *Evaluate `c1` to see what the tree looks like*. Which column did Pyret choose? @fitb{5em}{@ifsoln{`"tail"`}} @hspace{1em} Evaluate `dt-code(c1)` to *see the function* for this decision tree. Where have you seen this code before? @fitb{}{@ifsoln{it's the code for `tail-classifier`!}}

@n Now add adding the `sex` column to the list, and define your tree as `c2`. Which column did the algorithm choose for the root? @fitb{5em}{@ifsoln{`swim`}}

@vspace{1ex}

*Use `dt-code` to turn `c2` into a function, then copy-and-paste that function into the Definitions Area.*

@n Change the function's name from `classifier` to `swim-classifier`. Use `classify` to see how many rows it got right @fitb{}{8 rows}

== The Optimal 2-Level Tree for this Training Data
By changing the last argument to `build-tree`, you can have Pyret build trees with 2 levels (or more)!

@n Evaluate `c3 = build-tree(simple-animals, [list: "mammal", "tail", "swim"], "species", 2)`, then use `c3` to *fill in the decision tree* in the top left-hand corner of this page.

@vspace{1ex}

*Use `dt-code` to turn `c3` into a function, then copy-and-paste that function into the Definitions Area. Name it `next-classifier`.*

@n Why did Charlotte and Webs get classified as snails? @fitb{}{@ifsoln{Their values for "mammal", "tail", and "swim" were identical to the snails'}}

@n Why did Shadestar get classified as a dog? @fitb{}{@ifsoln{Since she swims, all three column values "looks" the same as the dogs}}

@n Compare the confusion-matrix of `swim-classifier` and `next-classifier`. What do you *Notice*? @fitb{}{} +
@fitb{}{}

== The Optimal Tree for this Training Data
@n Will a deeper tree built from *the same columns* do a better job? Circle your prediction before you try it out: @hspace{1em} Yes @hspace{1em} No

@n Try it out. Were you right? @fitb{2em}{@ifsoln{No}} @hspace{1em} Why or why not? @fitb{}{} +
@blanklines{2}{
@ifsoln{Looking only at these columns, Shadestar will *always* look like a dog, and Charlotte and Webs will *always* look like snails!}
}

@n What other column(s) should we consider, to improve our accuracy on this training dataset? @fitb{}{@ifsoln{sex - definitely not `species!`}}

@n Build your tree, this time adding the column(s) you listed above. How many levels deep will Pyret's optimal tree go? @fitb{}{3}