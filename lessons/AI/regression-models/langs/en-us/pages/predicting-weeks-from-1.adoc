= Predicting Weeks from Age _or_ Pounds
++++
<style>
/* custom positioning for subscript */
div#body sub { position: relative; bottom: 0.3em; }

/* have pyret studentAnswer match fitb on this page */
.studentAnswerShort { min-width: 5em; }

/* claw back some space from the table cells and autonum */
div#body td { padding: 0 !important; }
html div#body.workbookpage .autonum { padding-top: 0.9em; }
</style>
++++

@n For each of the scatter plots below, draw what you think are the best regression functions to predict `weeks`.

[cols="1a,1a", grid="none", frame="none"]
|===
| @image{../images/pounds-v-weeks.png, 325}
| @image{../images/age-v-weeks.png, 325}
|===

== The `pounds` Predictor

@n Find Kujo in the @online-exercise{https://www.desmos.com/3d/ip4ucm56mx, Pounds v. Weeks Desmos graph}.

@n Click and drag Kujo's data point to _rotate the 3d chart_. What do you *Notice* about the purple "age v pounds" points?

@blanklines{2}{
@ifsoln{They are all flattened against the z-y plane. All the x-values are zero. There's a purple plane at the bottom, along the x-y-plane}
}

*Refresh the page* to return the 3d chart to where it started. On your keyboard, press Ctrl-Alt-P (PC) or Ctrl-Option-P (Mac), then - just for a moment - tap the down-arrow and then left-arrow key, so that you can see the *smallest amount of purple* at the bottom of the graph.

@n Adjust the sliders for @math{m_{pounds}} and @math{b} to find the line of best fit. Write your answers below, in both function and Pyret form:

[cols="^1a, ^1a", frame="none", stripes="none", header="none"]
|===
| @math{weeks(pounds) = (}@fitbruby{5em}{}{@math{m_{pounds}}}@math{\times pounds) + }@fitbruby{5em}{}{@math{b}}
| @show{(code '(define (weeks pounds) (+ (* (?ANS m) pounds) (?ANS b))))}
|===


@n *Refresh the page* to return the 3d chart to where it started. *Click the folder icon* next to "Age v. Weeks v. Pounds (orange)" to turn on the 3d data, then drag to rotate the graph. Can you find Kujo's 3d point? What do you *Notice* about the orange, 3d points and the purple, 2d ones?

@blanklines{2}{
@ifsoln{The orange ones lines up with the purple ones when the x-axis is coming right out of the screen.}
}

== The `age` Predictor
@n Find Kujo in the @online-exercise{https://www.desmos.com/3d/glgrgcs4bp, Age v. Weeks Desmos graph}.

@n Click and drag Kujo's data point to _rotate the 3d chart_. What do you *Notice* about the blue "age v pounds" points?

@blanklines{2}{
@ifsoln{They are all flattened against the z-y plane. All the y-values are zero. There's a purple plane at the bottom, along the x-y-plane}
}

Rotate the graph using your arrow keys, so that you can see the *smallest amount of blue* at the bottom.

@n Adjust the sliders for @math{M_{age}} and @math{b} to find the line of best fit. Write your answers below, in both function and Pyret form:

[cols="^1a, ^1a", frame="none", stripes="none", header="none"]
|===
| @math{weeks(age) = (}@fitbruby{5em}{}{@math{m_{age}}}@math{\times age) + }@fitbruby{5em}{}{@math{b}}
| @show{(code '(define (weeks age) (+ (* (?ANS m) pounds) (?ANS b))))}
|===

@n *Refresh the page* to return the 3d chart to where it started. *Click the folder icon* next to "Age v. Weeks v. Pounds (orange)" to turn on the 3d data, then drag to rotate the graph. Can you find Kujo's 3d point? What do you *Notice* about the orange, 3d points and the blue, 2d ones?

@blanklines{2}{
@ifsoln{The orange ones lines up with the blue ones when the y-axis is coming right out of the screen.}
}

@n In the *Definitions Area*, fill in the slope and intercept for your predictor functions, and use `fit-model` to fit them to each scatter plot.