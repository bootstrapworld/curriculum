[.beta]
= Simple Regression Models

@description{Students are introduced to ALVINN — the world's first self-driving car — as a compelling example of supervised machine learning. They explore a driving simulator, collect training data, make scatter plots to find the best explanatory variable, and use Pyret's linear regression tools to build and evaluate predictor functions.}

@lesson-prereqs{data-driven-algorithms, fitting-models}

@keywords{}

[@lesson-intro-table]
|===
| Lesson Goals
| Students will be able to...
@objectives

| Student-facing Lesson Goals
|
- Let's learn how the world's first self-driving car was trained!
- Let's use regression to build a model that knows how to steer!

| Materials
|[.materials-links]
@material-links

| Relevant Resources
|
- @link{https://www.youtube.com/watch?v=ntIczNQKfjQ, "This two-minute video"} from the Carnegie Mellon University Robotics Institute} shows the first self-driving car in action.
|===

== Self-Driving Cars

=== Overview
Students are introduced to ALVINN, the world's first self-driving car, as a compelling example of supervised machine learning. They explore a driving simulator, collect their own training data, and discover that predicting steering angle is a regression problem.

=== Launch
@slidebreak{LaunchR}
@right{@image{images/self-driving-car.jpg, 300}}

Self-driving cars feel like the future has really arrived! Many people think they are one of the strongest examples of "AI" really thinking, instead of just "following an algorithm". After all, _how can a computer possibly make decisions about how to drive?_

@teacher{Give students time to discuss. Remind them about the AI Effect, and how people also felt when the first spell-checkers were developed!}

@slidebreak{Launch}

Self-driving cars work via a lot of inputs (sensors, cameras, etc.) and a lot of outputs (how much to accelerate, brake, and steer). Steering, for example, is incredibly difficult! Considerations include:

- Whether the road ahead bends to the left or right, and how sharply
- How far the car is from the center of the lane
- Whether the car is in-line with the lane, or heading towards one side
- Obstacles and traffic lights that could be in the way
- And lots more!

We're going to explore how one form of @vocab{supervised learning} -- regression, specifically -- allows cars to "know where they're going".

@slidebreak{LaunchR}
@right{@image{images/navlab.png, 300}}
*Meet ALVINN!*

ALVINN (short for Autonomous Land Vehicle in a Neural Network) was developed in 1989 by a team of computer scientists at Carnegie Mellon University. ALVINN is the forebear of today's self-driving cars: a retrofitted Army ambulance equipped with sensors, computers, and small motors to turn the wheel, press the pedals, etc. ALVINN had about one-tenth of the processing power of an Apple Watch -- but was able to safely drive on a very limited course at 70 miles per hour by the early 1990s! How did it happen?

@slidebreak{Launch}

As with all machine learning, ALVINN required @vocab{training}. The goal of that training? _Learning how to steer._ Here's what ALVINN's training entailed:

(1) A person drove the vehicle while a front-facing video camera recorded the road.

- In @vocab{supervised learning}, the human driver is often called the _supervisor_
- The supervisor demonstrates the correct behavior (e.g. which direction the steering wheel should be turned)

(2) Ten times each second, ALVINN recorded...

- a digitized image of the road ahead, along with a distance to the nearest obstacle
- the steering direction chosen by the driver at that moment

@slidebreak{Launch}

(3) ALVINN's training data (@vocab{training corpus}) can be represented as an input-output table, with road images and obstacle distances as inputs and the correct steering angles as outputs.

@indented{
[cols="^.^1,^.^1,^.^1", stripes="none", options="header"]
|===
| `road-image`                      | `obstacle-distance` (ft) | `steering-angle`
| @image{images/road1.png, 100}     |          21              |   0.02º
| @image{images/road2.png, 100}     |          49              |  55.20º
| @image{images/road3.png, 100}     |          37              | -25.43º
|===

@teacher{
Note: These images were selected for illustrative purposes. The 30x32 pixel images produced by ALVINN were of much lower quality!
}
}

@slidebreak{Launch}

(4) Using this dataset, ALVINN uses regression to generate a @vocab{function} for predicting the correct steering angle from the road image and obstacle distance.

@lesson-point{
`road-image` and `obstacle-distance` are the @vocab{explanatory variables}. `steering-angle` is the @vocab{response variable}.
}

(5) After training is complete, a human pushes ALVINN's "run" switch and the vehicle begins driving. As ALVINN drives, it continuously evaluates its learned function to produce steering angles — just as the human driver did during training.

@teacher{
If you want your students to see ALVINN in action, now is a good time to view @link{https://www.youtube.com/watch?v=ntIczNQKfjQ, "this brief two-minute video"}.
}

@slidebreak{Launch}

@QandA{
@Q{What information does ALVINN process before producing a steering angle?}
@A{ALVINN processes the image of the road in front of it, along with an obstacle distance, and evaluates its learned function to produce a steering angle.}

@Q{What did ALVINN learn _from_?}
@A{A human driver — the supervisor — who demonstrated correct steering. Those input-output pairs form the training corpus.}

@Q{What is ALVINN actually _computing_ when it decides how to steer?}
@A{It's evaluating a function: given the inputs (road image, obstacle distance), produce a steering angle. The function was learned from the training data.}

@Q{Why is this called _supervised_ learning?}
@A{Because a human provided the correct answers (steering angles) during training, supervising what the machine learned.}
}

@slidebreak{Launch}

In discovering the details of ALVINN's training, you may have noticed three key phases:

1. Human-tagged data is recorded, demonstrating the knowledge to be learned
2. A machine learning algorithm finds a *function* that maps from inputs to the correct outputs
3. That function is applied to new, unseen data

@lesson-point{
We're going to do exactly the same thing — training our own model to steer a virtual self-driving car!
}

=== Investigate
@slidebreak{Investigate-DN}

@lesson-instruction{
- Open @starter-file{self-driving} and click "Run".
- Turn to @printable-exercise{driving-game-exploration.adoc} and follow the instructions there.
}

@teacher{
Give students several minutes to play the simulator before debriefing. Students who finish early can try collecting data at different speeds, or deliberately drive poorly to see what bad training data looks like.
}

@slidebreak{Investigate}

@QandA{
@Q{What columns did you see in your training data table?}
@A{`speed`, `curve`, `skew`, `offset`, and `steering-angle`}

@Q{What is the difference between a positive and negative `steering-angle`?}
@A{Positive means turning right}
@A{Negative means turning left}
@A{(This could be vice versa for other simulators, depending on the simulator's convention.)}

@Q{How many rows of data did you collect in a few minutes of driving?}
@A{Answers will vary — probably 500 to 1000.}

@Q{Training a driving model requires the best possible data. Do you think that you or someone else could do a better job of driving the car around the track with more practice?}
@A{Probably!}
}

@slidebreak{Investigate}

@lesson-point{
We've prepared a @vocab{training corpus} for you containing expert driving data, collected across many different road conditions and driving speeds. It's already loaded in the starter file as `training-data`.
}

@QandA{
@Q{Looking at the `training-data` table, what is the thing we're trying to _predict_?}
@A{The `steering-angle` — how far to turn the wheel.}

@Q{What _type_ of value is a steering angle?}
@A{A number (degrees of rotation).}

@Q{What other data types do you see in this table?}
@A{None! All of the data columns are quantitative.}
}

=== Synthesize
@slidebreak{Synthesize}

@QandA{
@Q{Why do we refer to the process of training a self-driving car as supervised learning?}
@A{Because the target column of our dataset is a record of what humans would do when given the values in the other columns.}
}


== Predicting Steering Angle

=== Overview
Students identify candidate explanatory variables from the training corpus, make scatter plots to explore the relationship between each variable and `steering-angle`, and settle on `curve` as the strongest single predictor.

=== Launch

@slidebreak{Launch}

In the @lesson-link{fitting-models} lesson, we learned that a relationship between two variables shows up as a point cloud in a scatter plot, and that we can describe this relationship with a @vocab{model}.

- The @vocab{explanatory variable} (x-axis) is the input we're predicting _from_.
- The @vocab{response variable} (y-axis) is what we're trying to predict.

In this case, our response variable is `steering-angle`. The training data has several columns that could serve as an explanatory variable.

@slidebreak{Launch}
[cols="^1a,^3a", options="header", stripes="none"]
|===
| Column       | What it measures
| `speed`      | How fast the car is moving
| `curve`      | How sharply the road is curving (positive = right, negative = left)
| `skew`       | How far out of alignment the car's orientation is from the direction the road is headed
| `offset`     | How far the car is from the center of the lane
|===

@QandA{
@Q{Which of these do you expect to be the best predictor of steering angle, and why?}
@A{Answers will vary — this is meant to generate discussion and surface intuitions before the data speaks.}
}

@slidebreak{Launch}
Let's use some Data Science to find some evidence about what really _is_ the best explanatory variable for our `steering-angle`.

@QandA{
@Q{What data visualization works best for showing possible relationships between two quantitative variables?}
@A{Scatter plots}

@Q{What should we look for in our visualizations, to choose the best explanatory variable?}
@A{The strongest-looking relationship (points most-tightly clustered around a line) is the one that is most strongly connected to `steering-angle`}
}

=== Investigate
@slidebreak{Investigate-DN}

@lesson-instruction{
- Return to the @starter-file{self-driving}.
- Use `scatter-plot` to compare each of the four possible explanatory columns to `steering-angle`.
- Then complete @printable-exercise{steering-angle-scatter-plots.adoc}.
}

@teacher{
Circulate and encourage students to read the scatter plots critically. Push them to notice whether the relationship looks linear, how much scatter there is, and whether there are obvious outliers.
}

@slidebreak{Investigate}

@QandA{
@right{@image{images/steering-v-curve-on-offset-scale.png, 350}}
@Q{Which variable appeared to show the *strongest* relationship with `steering-angle`?}
@A{`curve` has a clear, nearly linear relationship. As the road curves more sharply in one direction, the steering angle increases proportionally.}
@A{Some students may notice that the scatter plot for `offset` has a nearly mirror image scatter plot to the `curve` data and think that it makes an equally good predictor (as the car is more offset in one direction, the steering angle decreases proportionally). Draw their attention to the fact that the x-axes ranges on `curve` and `offset` are quite different. When the `curve` scatterplot window is @math{-30 < x < 30} (matching the window for the `offset` plot), we see that data is much more tightly clustered.}

@Q{Which variable appeared to show the *weakest* relationship?}
@A{`speed` — there's a lot of scatter with no clear linear pattern.}

@Q{Did any of the relationships surprise you?}
@A{Answers will vary.}
}

@opt{If you'd like to give your students more practice with `fit-model` and interpreting @vocab{S-values}, you could have them define all 4 of their models in the starter file and evaluate their fit.}

=== Synthesize
@slidebreak{Synthesize}

@QandA{
@Q{Suppose you only had 2 rows of training data — and they fell in a perfectly straight line. Would you trust a model built from those 2 points?}
@A{No — two points can *always* be connected by a line. That doesn't mean the pattern is real or will generalize.}

@Q{How much data do you think you'd need before you could trust a model?}
@A{There's no single answer, but more data collected across a wider variety of conditions makes a model more trustworthy.}

@Q{Is size-of-dataset the only important factor in what makes a good training set?}
@A{No - we also need representative data! A self-driving car needs to be built on data from all kinds of different roads, weather, offsets, times of day, road sizes, speeds, traffic density, etc.}
}


== Linear Regression

=== Overview
Having already manually fit linear model to the  data, students use Pyret's `lr-plot` to find the optimal fit for the data. They interpret the model's coefficients, use `mr-code` to generate row-consuming predictor functions, and test their predictors using the `drive` function. Finally, they reflect on what it means to have trained a model.

=== Launch
@slidebreak{Launch}

On @printable-exercise{steering-angle-scatter-plots.adoc} you estimated the @vocab{slope} @math{m} and @vocab{y-intercept} @math{b} of the lines you fitted to the scatter plots. But how do we know whether or not they were the _best_ linear model: the @vocab{line of best fit} for the data?

@slidebreak{Launch}

Data scientists use a method called @vocab{linear regression} to find the straight-line pattern that describes a set of inputs and an already-known target output with the mathematically _optimal_ values of @math{m} and @math{b} - that minimizes error as much as possible.

- This form of learning — where we already know the correct output for each input — is called @vocab{supervised learning}.
- In Machine Learning, the act of finding a function from the data is called @vocab{training}.

@slidebreak{Launch}

Pyret's `lr-plot` function computes and visualizes the @vocab{line of best fit}. Its contract is the same as `scatter-plot`:

@show{(contract "lr-plot" '((t Table) (labels Str) (xs Str) (ys Str)) "Image")}

@slidebreak{Launch-DN}
@lesson-instruction{
- Turn to the first section of @printable-exercise{lr-steering-angle.adoc}.
- Use `lr-plot` on `training-data` to find the best-fit line for each of the variables as compared to `steering-angle`.
- How do Pyret's optimal models for the data compare to the ones you fit by hand?
}

@teacher{
Students will notice that `lr-plot` defines models down to decimal values, certainly a level of specificity more than their hand-drawn model. This motivates the idea that the computer can optimize this much better and faster than humans — which is, after all, the point of machine learning.
}

@slidebreak{InvestigateR}
@QandA{
@right{@image{images/steering-v-curve-lr.png, 300}}
@Q{What does the slope of our `curve` model tell us?}
@A{That the steering angle increases by about 19.5 degrees for each 1 degree increase in the curve.}

@Q{Does the y-intercept make sense in the real world? Why or why not?}
@A{Yes. If the curve is zero, the road is straight and the steering angle should be zero.}

@Q{What does the @math{S}-value tell us?}
@A{We expect the error in predictions made by this model to be off by about 37 degrees.}

@Q{Based on the @math{S}-value, would you expect the `curve` model to successfully be able to "drive" a full lap around a course in the game?}
@A{Probably not! 37 degrees is a pretty large error.}

@Q{Do our predictor functions expect steering angle to increase as our other variables increase?}
@A{No! Steering angle is expected to decrease as `skew` and `offset` increase.}
}

=== Investigate

@slidebreak{Investigate}

Let's see how well our predictors drive the car! Pyret has a `drive` function:

@show{(contract "drive" '((predictor ("Row" "Number"))) "Table"
"runs the driving simulator, using predictor to compute the steering angle at each frame")}

@lesson-instruction{
- Complete #7 and #8 on @printable-exercise{lr-steering-angle.adoc}.
- Spend some time thinking about what the error message you get might be telling you!
}

@slidebreak{Investigate}
Notice that `drive` takes a *Row-consuming* function — but `curve-predictor` consumes *Numbers*, not Rows!

@QandA{
@Q{Why does `drive` need a Row-consuming function instead of a Number-consuming one?}
@A{Because at each moment, `drive` has access to the entire row of data — not just one column. A Row-consuming function can decide which columns to look at.}

@Q{How can we get a Row-consuming version of `curve-predictor`?}
@A{We need a new Pyret function that generates Row-consuming code from our training data.}
}

@strategy{Want Deeper Programming?}{

If you're teaching this in the context of a CS, CTE, or any other class where programming is an important learning outcome, have students figure out how to make their `curve-predictor` function take a Row instead of a Number. This is a great exercise about abstraction and the idea of a data structure, and it uses programming tools they've seen in prior lessons.

If your learning goals are more about AI Literacy or Mathematics, this would make a good challenge exercise for an advanced student. Otherwise, it's okay for you to move on to `regression-model-code`, which will write the Row-accessing code for them.
}

@slidebreak{Investigate}

We could write it by hand, but Pyret has exactly the right tool:

@show{(contract "regression-model-code" '((t Table) (inputs List<Str>) (prediction Str)) "Code"
"produces code that defines a trained, row-consuming model (copy/paste into the Definitions Area!)")}

For example: `regression-model-code(training-data, [list: "curve"], "steering-angle")`

@lesson-instruction{
- Complete @printable-exercise{lr-steering-angle.adoc}.
- Use the code you just considered to complete the `curve-predictor2` template in your Definitions area and click "Run" to load it. Then evaluate `drive(curve-predictor2)` in the Interactions Area.
- Use `regression-model-code` to generate a row-consuming predictor for each of the other variables.
- Update the templates for `offset-predictor`, `skew-predictor`, and `speed-predictor`. Click "Run" to load them. 
- Try each one with `drive(...)`.
}

@teacher{
Students will find that `curve-predictor` drives much better than the others. Allow time for them to try all four and compare results before debriefing.
}

@slidebreak{Investigate}

@QandA{
@Q{Which predictor drove the best? Did that match your predictions from the scatter plots?}
@A{`curve-predictor` — yes, it had the strongest linear relationship with `steering-angle`.}

@Q{Did the predictor "drive" better than you did? Why?}
@A{No!}
@A{When we were driving, we could take all of the variables into account and the predictor was only using one.}
}

=== Common Misconceptions
- *More steering isn't always better.* Students may expect that a predictor trained on "good" data will drive perfectly. Remind them that a model can only generalize from what it's seen — it has no understanding of the road.

=== Synthesize
@slidebreak{Synthesize}

@lesson-point{
Congratulations — you have just *trained a machine learning model!*
}

@QandA{
@Q{What did "training" mean in this context?}
@A{We provided the computer with a dataset of input-output pairs (curve → steering angle), and it found the function that best maps from input to output.}

@Q{Suppose the car encounters a curve steeper than any in the training data. What will the predictor do?}
@A{It will extrapolate from the pattern it learned — which may or may not be accurate. It has no way of knowing it's outside its training range.}

@Q{What if our training data was all collected on dry roads, and the car now drives on ice?}
@A{The model was never trained on those conditions. It will predict as if conditions are normal — which could be very wrong.}

@Q{When an AI system confidently produces a wrong answer because the input is outside its training data, that's sometimes called a _hallucination_. How does what you just built connect to that idea?}
@A{Our predictor will confidently output a steering angle for ANY input — even situations it's never seen. It doesn't know what it doesn't know.}
}

@slidebreak{Synthesize}

@QandA{
@Q{Could we train a model using more than one explanatory variable?}

@Q{What would a line-of-best-fit look like on a scatter plot with _two_ explanatory variables?}

@A{Would we have one slope and intercept? Two?}
}


@strategy{Computers aren't people!}{

In conversations about AI, computers often get anthropomorphized — given human-like traits when they are not in fact human. This can block students from understanding the mechanics of machine learning.

Avoid saying that the model "knows" how to drive, or that it "understands" the road.

A model's output is always the result of *mathematical computation* — evaluating a learned function on an input. All supervised machine learning relies on data, functions, and computation.
}
