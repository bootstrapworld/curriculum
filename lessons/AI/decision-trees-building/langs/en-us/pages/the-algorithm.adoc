= Building a Decision Tree: The Algorithm
++++
<style>
div#body #preamble_disabled { flex-grow: 0 !important; }
div#body .fitb { min-width: 2em !important; padding-top: 0.7em !important;}
div#body .autonum, .fitb.stretch::after { padding-top: 0.7em !important; }
div#body table.fit-content tr td { padding: 0.3em 0.625em !important; }
div#body table { margin: 0 !important; }
</style>
++++

[%autowidth, cols="^,^,^,^", frame="none", stripes="none", options="header"]
|===
| Split on
| "no" leaf (most common in this group)
| "yes" leaf (most common in this group)
| # Correct

| `r["tail"] == true`
| "snail" (2 out of 4)
| "cat" (5 out of 11)
| 7
|===

*Use this table as a model for completing the Categorical and Quantitative Splits sections below.* Notice:

- the 1st column is the code that will ask our question: "Does the animal have a tail?"
- the 2nd and 3rd columns show predicted species for animals with and without tails (and the numbers these predictions were based on)
- the last column tell us how many of these predictions would be correct for the animals in our testing table

@vspace{1ex}

== Categorical Splits
@n Complete the table below, using `image-dot-plot` to see the distribution of `mammal`, `tail`, `swims` and `sex`.

[%autowidth, cols="^,^,^,^", frame="none", stripes="none", options="header"]
|===
| Split on
| "no" leaf (most common)
| "yes" leaf (most common)
| # Correct

| `r["mammal"] == true`
| "snail" or "tarantula" (2 of 4)
| "cat" (5 of 9)
| 7

| `r["tail"] == true`
| @ifsoln{"snail" or "tarantula" (2 of 4)}
| @ifsoln{"cat" (5 of 11)}
| @ifsoln{7}

| `r["swims"] == true`
| @ifsoln{"cat" (4 of 9)}
| @ifsoln{"dog" (4 of 6)}
| @ifsoln{8}

| `r["sex"] == "hermaphrodite"`
| @ifsoln{"cat" (5 of 13)}
| @ifsoln{"snail" (2 of 2)}
| @ifsoln{6}

| `r["sex"] == "female"`
| @ifsoln{"dog" or "snail" or "cat" (2 of 8)}
| @ifsoln{"cat" (3 of 7)}
| @ifsoln{6}
|===

@n Why wouldn't it make sense to have a second row for `r["mammal"] == false`? @fitb{}{} +
@fitb{}{@ifsoln{For Boolean columns where there are only two options the split is identical whether we phrase the question as IS or ISN'T.}}

== Quantitative Splits
@n Complete the table below, using `image-dot-plot` to see the distribution of `pounds`.

[%autowidth, cols="^,^,^,^", frame="none", stripes="none", options="header"]
|===
| Split on
| "no" leaf
| "yes" leaf
| # Correct

| `r["pounds"] > 2`
| "snail" or "tarantula" (2 of 4)
| "cat" (5 of 11)
| 7

| `r["pounds"] > 6`
| @ifsoln{"snail" or "tarantula" (2 of 4)}
| @ifsoln{"cat" or "dog" (4 of 9)}
| @ifsoln{6}

| `r["pounds"] > 9.5`
| @ifsoln{"cat" (4 of 10)}
| @ifsoln{"dog" (4 of 5)}
| @ifsoln{8}
|===

== Completing the Tree

@n Choose your split and explain your choice: @fitb{}{@ifsoln{`r["swims"] == true` or `r["pounds"] > 9.5` both lead to the most correct classifications}}.

@n Fill in the root at the top of the tree below, and label the edges with the number of animals in each sub-group. Place a check next to each row of the table that your split predicts correctly.

[cols="3a, 2a", frame="none", border="none", grid="none"]
|===
|
[.data-table, cols="1,^1,^1,^1,^1,^1,^1", stripes="none", options="header"]
!===
! name 			! species	 ! sex				! pounds! tail	! mammal! swims
! "Firepaw"		! "cat" 	 ! "female"			! 9.5	! TRUE	! TRUE	! FALSE
! "Leafpool"	! "cat" 	 ! "female"			! 10.6 	! TRUE	! TRUE	! FALSE
! "Shadestar"	! "cat" 	 ! "female"			! 9.0 	! TRUE	! TRUE	! TRUE
! "Bramble"		! "cat" 	 ! "female"			! 4.1 	! TRUE	! TRUE	! FALSE
! "Brick"		! "cat" 	 ! "male" 			! 8.5 	! TRUE	! TRUE	! FALSE
! "Frisky"		! "dog" 	 ! "female"			! 9.6	! TRUE	! TRUE	! TRUE
! "Socks"		! "dog" 	 ! "female"			! 11.1 	! TRUE	! TRUE	! TRUE
! "stripes"		! "dog" 	 ! "male" 			! 10.0 	! TRUE	! TRUE	! TRUE
! "Snickers"	! "dog" 	 ! "male" 			! 10.3 	! TRUE	! TRUE	! TRUE
! "Bruni"		! "lizard" 	 ! "female" 		! 8.2 	! TRUE	! FALSE	! TRUE
! "Jesús"		! "lizard"	 ! "male" 			! 3.5 	! TRUE	! FALSE	! FALSE
! "Gary"		! "snail"	 !"hermaphrodite"	! 0.1 	! FALSE	! FALSE	! FALSE
! "Turbo"		! "snail"	 !"hermaphrodite"	! 0.1 	! FALSE	! FALSE	! FALSE
! "Charlotte"	! "tarantula"! "male" 			! 0.4 	! FALSE	! FALSE	! FALSE
! "Webs"		! "tarantula"! "female" 		! 0.4 	! FALSE	! FALSE	! FALSE
!===

|
@image{../images/empty-tree.png}
|===
