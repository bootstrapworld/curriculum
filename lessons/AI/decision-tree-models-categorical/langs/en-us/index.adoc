[.beta]
= Categorical Decision Trees

@description{Students explore an approach to recommending videos and predicting election outcomes. They develop and analyze decision trees to predict individuals’ purchasing decisions.}

@lesson-prereqs{data-driven-algorithms, dot-plots}


@keywords{}

[@lesson-intro-table]
|===
| Lesson Goals
| Students will be able to...
@objectives

| Student-facing Lesson Goals
|

- Let's create, use, and understand decision trees, a model used to make recommendations and predict outcomes 

| Materials
|[.materials-links]
@material-links


|===

== Predicting Categories @duration{25 minutes}

@objective{ai-create-tree}

=== Overview

Students consider technologies that make predictions and recommendations, and then play a game to help them build a frame of reference for diagramming the decision-making process. By the end of this section, students should know:

- What a decision tree is
- That more than one tree can be constructed for a given dataset
- That decision trees can be "good" or "not good" depending on their shape
- That decision trees can be "good" or "not good" depending on the frequency of categories

=== Launch
@slidebreak{Launch}

Consider each of the three situations described below:

- You take an online quiz, which guesses your hairstyle.
- Two individuals who work for the same company and earn the same salary apply for a loan by filling out the same online form at the local bank. One of them is granted a loan, but the other is not.
- A news anchor displays a map with some states colored blue and others colored red. She explains that the map's predictions for the upcoming election were developed using historical census data.
- A camera at the bank captures a picture of your face, immediately identifying who you are.

What do all of these examples have in common?

@slidebreak{Launch}

On the surface, these situations may not seem to have a lot in common. But all of these are examples of @vocab{classifiers}, a kind of function that:

@indented{
(1) Consumes some data

(2) Asks questions about that data

(2) Use the answers to produce a predicted outcome from a list of categories
}

@QandA{
@Q{What data do you think is used for *guessing your hairstyle*?}
@A{Viewing history, search history, channel subscriptions, likes, dislikes, google activity, "not interested" feedback}

@Q{What data do you think is used for *approving loan requests*?}
@A{Debt, assets, payment history, number of open accounts, delinquent accounts, income/revenue, expenses, overdrafts in the last 90 days}

@Q{What data do you think is used to *predict election outcomes*?}
@A{Census data including voter race, gender, income level, party affiliation, and age, as well as prior years' results}
}

@slidebreak{Launch}

All categorical @vocab{classifiers} are functions that map inputs to a specific set of categories.

[.data-table, cols="1a, 2a, 3a", options="header"]
|===
| Example 			| Input Data _(known)_				| Categories _(unknown)_
| Hairstyle Quiz	| Sex, age, race, music, ZIP... 	| Ponytail vs. Pixie cut vs. Dreadlocks vs. Mohawk...
| Bank Loan 		| Income, job, age, credit score... | Approved vs. Not-Approved
| Elections 		| Past voting, recent polls...	 	| Red vs. Blue
| Facial Recognition| Your face 						| Every person in America
|===

For example, the hairstyle quiz can probably rule out "ponytail" if the sex of the person taking the quiz is "male".  A classifier's job to make the best guess possible, and it won't always be right! After all, some people with ponytails are male!

@lesson-point{No classifier is perfect. _Some_ are useful.}

=== Investigate
@slidebreak{Investigate}

@vocab{Classifiers} use something called a @vocab{Decision Tree}, asking questions in order to narrow down the possible outcome categories until it reaches a single, predicted outcome.

If you've ever played the game "I spy", you've already got some practice building decision trees!

The rules are pretty simple:

- A player comes up with something they is visible to everyone playing
- They come up with a hint, to help narrow the possible answers. For example: "I spy with my little eye...something blue!"
- The other players ask yes-or-no questions to help them guess what it is

@slidebreak{Investigate}

@lesson-instruction{
@right{@image{images/tree-1.png}}
We're going to play a version of "I Spy", called "I Spy - Classify!"...

- Everyone will stand a their desks (or front of the room, etc)
- I'm going to choose one student as the secret, but keep the choice to myself
- All of you will take turns asking yes-or-no questions. For example, "are they wearing glasses?"
    - If I answer "yes", every student wearing glasses stays up, while the others return to their seats
    - If I answer "no", every student *not* wearing glasses stays up, while the others return to their seats
- I'll keep track of our progress on the board
}

@slidebreak{Investigate}

*I Spy*...a student in this class. Who am I thinking of? *Classify!*

@teacher{
@right{@image{images/tree-2.png}}

- Let your students ask questions one at a time. Write each question on the board in the form of a flow-chart, with "yes edges" and "no edges" (lines labeled with "yes" or "no") branching out to the left and right.
- On each edge, write the number of students for whom each answer is "yes" and "no".
- For now, most of the tree will be empty: your students won't need to ask questions about those who already sat down!
}

@slidebreak{Investigate}

Congratulations! You were able to classify the student I had in mind, by asking just a few questions.

@QandA{
@Q{How many questions did it take for you to classify this student?}
@Q{Suppose you all just asked "are they named XYZ?" over and over, until we classified the student? Would you eventually have found the answer?}
@A{Yes}
@Q{Why didn't we do that?}
@A{It probably would have taken longer! Asking about groups helped us eliminate students and narrow down our choices faster.}
}

Let's play a few more rounds. This time, however, we can only _add_ to our @vocab{Decision Tree}: for everything else, we need to ask the same questions in the same order.

@teacher{
Play a few more rounds. Try to pick students that will force the class to add to their tree! For each student they classify, ask the class how many questions it took to find them.
[cols="^.3a,^2a,3a", grid="none", frame="none", stripes="none"]
|===
| @hspace{7em}**Round 1**
|
| @hspace{6em}**Round 2**

| @image{images/tree-2.png}
| @image{images/arrow.png, 50}
| @image{images/tree-3.png}
|===
}

@slidebreak{Synthesize}

@QandA{
@Q{In this game, what were our "categories"?}
@A{Students in the class}
@Q{How many categories did we have?}
@A{The number of students in the class}
@Q{If you could start over, how would you change this @vocab{decision tree}?}
@A{This is a big question! Give students some time to discuss}
@Q{How do you know if a question is a good one to start with or not?}
@A{Good questions help us eliminate many of the remaining categories whether the answer is yes _or_ no}
@Q{Suppose our first question was "has glasses?", and all but one student in the class wore glasses. How many questions would it take if I was thinking of the student who wears glasses?}
@A{Just one! If the answer is "yes", we know exactly who it is!}
@Q{Would that be the best question to start with, if we were going to play the game many times?}
@A{No, because if the answer is "no" we're basically still starting at the beginning}
}
@slidebreak{Synthesize}

@QandA{
@Q{How does someone use a decision tree to make predictions?}
@A{Ask the question at the root of the tree, then follow the branches to ask additional questions until we get to a category at the end of a path.}
@Q{Suppose we were playing "Classify", but now you have to guess which *country* I'm thinking of. What might be a good starting question?}
@A{Answers will vary, but look for answers that eliminate groups of countries quickly.}
@A{Is it a country in North America?}
@A{Is it a country where people speak Spanish?}
@A{etc...}
@Q{If we want to get to the correct categorization as quickly as possible, what would we want to be true about the first question we ask?}
@A{We would want it to split the list of options as evenly as possible to guarantee eliminating a significant number of options right off the bat.}
}


== Building Decision Trees from a Training Dataset

@objective{ai-create-tree}

=== Overview
Students refine their intuition for building decision trees, discovering how they are constructed from training datasets.

=== Launch

@slidebreak{LaunchR}
@right{@image{images/tree-3.png}}
A @vocab{decision tree} is like an upside down tree that shows decisions and their predicted outcomes. The diagram of our "Classify" game is a decision tree! The examples we talked about at the beginning (Guess-the-haircut, Loan approvals, Election predictions, and facial recognition) all use decision trees.

You've learned that some @vocab{decision trees} are better than others. For example, the _frequency of each category_ affects the "best node" ("if everyone has glasses...").

Machine-learning algorithms generate decision trees from training @vocab{datasets}, using the column headers as questions. Each node in the tree splits the dataset, allowing us to make the best prediction we can about some outcome.

*Could we guess the species of an animal, without actually _seeing_ the `species` column?*


@slidebreak{LaunchR}

@right{@image{images/terminology-tree.png, 300}}

- A @vocab{decision node} splits a dataset around the values in one of its columns. The attribute in the column serves as the "question" that is being asked.
- The @vocab{root node} is the very top @vocab{decision node}. It represents an entire dataset.
- @vocab{Splitting} is the process of creating branches and additional nodes corresponding to subsets of a dataset based on values in one column. (We're sticking to splits that only create two branch nodes, but some decisions trees use splits that create more than two.)
- A @vocab{leaf node} is a node that contains the predicted outcome. These outcomes come from a designated column in the original dataset. Leaf nodes have no outgoing branches.
- The sequence of questions from the root node to an outcome is called a path.

@teacher{Discuss the decision tree you made during your "Classify" game to help students identify the root node, decision nodes, leaf nodes, and paths on the tree so far.
}

@slidebreak{Launch-DN}

We're going to play another round of "I Spy - Classify", but this time you're going to use a real dataset.

[.data-table, cols="1,^1,^1,^1,^1,^1", stripes="none", options="header"]
|===
| name        | species     | sex           | tail    | mammal | swims
| "Firepaw"   | "cat"       | "male"        | true    | true   | false
| "Frisky"    | "dog"       | "female"      | true    | true   | true
| "Socks"     | "dog"       | "female"      | true    | true   | true
| "Stripe"    | "dog"       | "male"        | true    | true   | true
| "Snickers"  | "dog"       | "male"        | true    | true   | true
| "Leafpool"  | "cat"       | "female"      | true    | true   | false
| "Shadestar" | "cat"       | "female"      | true    | true   | true
| "Bramble"   | "cat"       | "female"      | true    | true   | false
| "Brick"     | "cat"       | "male"        | true    | true   | false
| "Bruni"     | "lizard"    | "female"      | true    | false  | true
| "Jesús"     | "lizard"    | "male"        | true    | false  | true
| "Gary"      | "snail"     |"hermaphrodite"| false   | false  | false
| "Turbo"     | "snail"     |"hermaphrodite"| false   | false  | false
| "Charlotte" | "tarantula" | "male"        | false   | false  | false
| "Webs"      | "tarantula" | "female"      | false   | false  | false
|===

@QandA{
Open @starter-file{decision-tree}, and click "Run".
@Q{Looking at the Definitions Area, what is the name of the table being defined here?}
@A{`simple-animals`}
@Q{What columns does this table have?}
@A{`name, species, sex, tail, mammal`, and `swims`}
@Q{Type `simple-animals` in the Interactions Area, to see the table. What species are here?}
@A{Dogs, Cats, Lizards, Snails, and Tarantulas}
@Q{What types of data do you see in this table?}
@A{Strings and Booleans}
@Q{Share at least one thing that you *Notice* about this dataset.}
@A{Shadestar is a cat who swims!}
@A{Charlotte is a male tarantula!}
}

@slidebreak{Investigate}
@QandA{
@Q{In Pyret, how could I make a bar chart showing the distribution of `species` in this dataset?}
@A{`pie-chart(simple-animals, species)`}
@Q{How could I get a _count_ of the number of mammals and non-mammals represented here?}
@A{`count(simple-animals, "mammal")`}
@Q{In the Definitions Area, what does `firepaw = row-n(simple-animals, 0)` do?}
@A{`firepaw` is being defined as the first row in the table}
@Q{What do I type in the Interactions Area, to see that row?}
@A{`firepaw`}
@Q{After clicking "Run", how can I get the `swims` value out of the first row in the table?}
@A{`firepaw["swims"]` *OR* `row-n(simple-animals, 0)["swims"]`}
@Q{What code would tell me if the first row is a mammal?}
@A{`firepaw["mammal"] == true` *OR* `row-n(simple-animals, 0)["mammal"] == true`}
*Define a new row, called `frisky`, to be the _second_ row in the table*
}

=== Investigate
@slidebreak{InvestigateC}
|===
| @image{images/tail-classifier-code.png}}
| @image{images/1-split-animals.png}
|===

Remember, a @vocab{classifier} is _just a function!_

- It consumes some input and produces some output
- Each input will only ever have one output (no matter how many times we classify `firepaw`, it will always guess the same species!).

@lesson-instruction{
In the Definitions Area, we've written a classifier function for you!
- Take a moment to read the code for `tail-classifier` carefully. Use the @vocab{decision tree} to guide you.
- What do you *Notice* about this code?
- What do you *Wonder* about this code?
- Talk with your partner or group about what it might be doing.
- This is probably the first time you've seen code like this! What questions do you have about it?
}

@teachers{Give students ample time to discuss and share back!}

@slidebreak{Investigate}
You've probably noticed that - just like any other function - `tail-classifier` has a contract!

@QandA{
@Q{What is the *name* of the function?}
@A{`tail-classifier`}
@Q{What is it's *domain*?}
@A{`Row`}
@Q{What is it's *range*?}
@A{String}
@Q{How can I test out this classifier with my `firepaw` row?}
@A{`tail-classifier(firepaw)`}
@Q{Can you explain why we got the result we did?}
@A{The function only checks to see if the animal has a tail. If it does, it produces `"cat"`}
@Q{What do you think we'll get back if we try the classifier on `frisky`? Why?}
@A{`frisky` (the second row in the table) has a tail. The classifier thinks everything with a tail is a cat!}
}

@slidebreak{InvestigateR}
@ifslide{@right{@image{images/1-split-animals.png}}}

If our tree ended here, we could still make a "best guess" about the species of row.

- The "yes" branch has more cats than anything else (5 out of 11), so "cat" is our best possible choice for a leaf. Our classifier will guess right about 45% of the time!
- The "no" branch us evenly split between snails and tarantulas, so we could flip a coin and pick one - no matter what we choose, our classifier will be right 50% of the time!

We've provided a `classify` function. Here's the contract:
@show{(contract "classify" '("Table" "String" "(Row -> String)") "Table")}

`classify` consumes a `Table` of rows to be classified, a `String` to tell it which column is being predicted, and a `(Row -> String)` classifier function. Try it out:

@indented{`classify(simple-animals, "species", tail-classifier)`}


@slidebreak{Investigate}

What root node would _you_ pick?
@lesson-instruction{
- Turn to @printable-exercise{building-decision-trees.adoc}, and complete *the first section*.
- When you're done, change the `my-classifier1` function in Pyret to match your 1-level @vocab{decision tree}.
- Try using it with the `classify` function. How did it do?
}

@teacher{Have students share what their root node was, why they chose it, and whether they were able to do better than the `tail-classifier`}

@lesson-instruction{
Fortunately, we don't have to stop at trees that are just one level deep!
- Complete *the second section* on @printable-exercise{building-decision-trees.adoc}.
- When you're done, change the `my-classifier2` function in Pyret to match your 2-level @vocab{decision tree}.
- Try using it with the `classify` function. How did it do?
}

@slidebreak{Investigate}
How do we know if a @vocab{decision tree} makes a good @vocab{classifier} or not? Using the `classify` function only tells us how many we got right, and how many we got wrong.

It's not enough to know that a classifier messed up 6 times! Data Scientists and Machine Language Engineers want a full, detailed "classifier report card" showing _which mistake_ it made! This report card a @vocab{Confusion Matrix}.

@QandA{
@Q{Out of the 5 cats in this dataset, how many did your best classifier classify _correctly_?}
@Q{Roughly what *percentage* of the cats is that?}
@Q{Out of the 2 snails in this dataset, how many did your best classifier classify _correctly_?}
@Q{Roughly what *percentage* of the snails is that?}
}

@slidebreak{Investigate}
A @vocab{confusion matrix} typically shows _percentages:_ 4-out-of-5 cats becomes 80%, 2-out-of-2 lizards becomes 100%, and so on.

We've created a function that does just that! Here's the contract:

@show{(contract "confusion-matrix" '("Table" "String" "(Row -> String)") "Table")}

Try it out with the original `tail-classifier`:

@indented{`confusion-matrix(simple-animals, "species", tail-classifier)`}

@slidebreak{Investigate}

[.pyret-table, cols="1a,^1a,^1a,^1a,^1a,^1a", options="header"]
|===
|actual-species|predicted-cat|predicted-dog|predicted-lizard|predicted-snail|predicted-tarantula
|"cat"|1|0|0|0|0
|"dog"|1|0|0|0|0
|"lizard"|1|0|0|0|0
|"snail"|0|0|0|1|0
|"tarantula"|0|0|0|1|0
|===

The best possible matrix has a diagonal line of `1`s, running from the top-left to the bottom right. That would mean that 100% of the cats were classified as cats, 100% of the dogs were classified as dogs, and so on....

How did your 2-level classifier do? Compare your confusion matrices with the people around you.

=== Synthesize
@slidebreak{Synthesize}
@QandA{
@Q{Will a decision tree always have the same number of leaf nodes as there were rows in the training dataset? Why or why not?}
@A{No. Generally multiple rows of a training dataset will have the same prediction. The training dataset we saw in this lesson section contained multiple knives, for example.}

@Q{Can a decision tree have the _fewer_ leaf nodes than there are unique categories in the training dataset? Why or why not?}
@A{Yes. A decision tree may not be deep enough to correctly classify every category - and in some cases, there may not *be* a way to correctly classify every category!}

@Q{Explain what the decision tree and table for the training dataset have to do with one another.}
@A{The column headers from the table are the questions that will get asked in the decision nodes of the decision tree.}
@A{The data in each column are the answers to those questions which become the arrow labels of the decision tree ("yes" and "no", for this dataset).}
@A{The output categories from the table are the leaf nodes in the decision tree.}
}

== Building Decision Trees in Pyret

=== Overview
Now that they know the algorithm, students explore Pyret's ability to build decision trees automatically! They explore the impact that different depths or columns have on the quality of a classifier.

=== Launch
@slidebreak{Launch}
By now, you know that not all @vocab{decision trees} are created equal! You also know the algorithm to compute an _optimal tree_, so you could generate a @vocab{decision tree} for any dataset.

For example, you could help a store figure out which customers are most likely to buy a particular video game:

[.data-table, cols="2,2,3,2,2", stripes="none", options="header"]
|===
| name 		| age      	 | shopping history 	| interest in game  | buys game
| Jan 		| "teens"    | "previous customer" 	| false				| false
| Noah		| "thirties" | "previous customer"	| false 			| true
| Sydney 	| "thirties" | "previous customer" 	| true 				| true
| Mariana 	| "thirties" | "new customer" 		| true 				| false
| Rasula	| "twenties" | "new customer"		| true 				| true
| Jillian	| "teens"    | "previous customer" 	| false 			| false
| Ariella	| "teens"    | "new customer"  		| true  			| true
| Isabela	| "thirties" | "previous customer"	| true 				| true
|===

If you've ever wondered how a company knows which coupons or promotions to send you, or which ads to show you on the web...now you have an idea!

@slidebreak{Launch}

You could write a simple spam filter, to get rid of unwanted email:

[.data-table, cols="^1,^1,^1", stripes="none", options="header"]
|===
| sender in address book	| seen sender before      | mass email
| true 						| true 					  | true
| true 						| false 				  | false
| false 					| true 					  | true
| false 					| false 				  | false
| true 						| true 					  | true
| true 						| false 				  | false
| false 					| false 				  | true
|===

If you've ever wondered how a spam filter works...now you have an idea!

@slidebreak{Investigate}
But computing a decision tree by hand takes a long time, especially if you're dealing with thousands of rows and dozens of columns! Fortunately, computers are _really good_ at following algorithms.

We've provided a function that will build a decision tree for you! Here's the contract:

@show{(contract "build-tree" '("Table" "String" "Number") "DecisionTree")}

The `build-tree` function takes three things in its Domain:

- The _training dataset_ from which to fit the tree
- The _label_ column containing all the "answers" for the various categories
- The _maximum depth_ the tree is allowed to be

@lesson-instruction{
- Complete @printable-exercise{computing-decision-trees.adoc}
}

=== Synthesize
@slidebreak{Synthesize}

@QandA{
@Q{What are some reasons that a decision tree might produce an inaccurate prediction or recommendation?}
@A{The prediction might represent closer to 50% of the training population rather than 100% of the population.}
@A{The training dataset might not completely represent the broader population, either in not having enough variables (columns) or enough samples (rows).}
@A{The training dataset could be inconsistent, in that the labeled outcomes for two rows are different while the rest of the columns have the same values.}

@Q{After testing our tree, we discovered that it was not as accurate as we had expected or hoped. Can you think of concrete, real-world examples of how a training dataset might under represent a population?}
@A{Responses will vary.}
@A{Medical systems being trained on only adults, but not children.}
@A{Tutoring systems trained on only native speakers of a language.}
@A{Music recommendation systems being trained on a narrow set of genres.}

@Q{We collapsed specific ages into categories (teenager, etc) to align the specificity of our data with the specificity of recommendations that we wanted from the decision tree. What if we had not done that, and instead created a branch for each individual age? How would individual branches have affected the decision tree and its recommendations?}
@A{The tree would have had many more branches and decision nodes.}
@A{We would likely have needed more training data with more samples at each age to get to acceptable accuracy.}

@Q{You have learned that @vocab{supervised learning} includes three steps: (1) demonstration of the learning process, (2) function abstraction, and (3) using the function. Describe what each step includes for the @vocab{supervised learning} of a @vocab{decision tree}.}
@A{Demonstration: For decision trees, the demonstration is the labeling of the data so that the computer learns the desired outcome (or "correct answer") for each data point. During the lesson, we knew whether the individuals in our training set would buy the video game.}
@A{Function abstraction: Creating the decision tree yields a model of how variables affect outcomes.}
@A{Use: Follow the decision tree to find the predicted outcome for a new input (set of values for the question variables).}

}



Creating a decision tree is a form of @vocab{supervised learning}, because the training data already contains the desired outcomes (as one column), and the algorithm just learns a function that maps from input to outcomes.
