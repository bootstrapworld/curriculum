= Image Similarity

@lesson-instruction{For this page, you'll need @starter-file{ai-image} open on your computer}

== Describing Images with Numbers

@n In the Interactions Area, evaluate `lesson-folder`, `img1` and `img2`

@n What is being defined on line 3 of this file? @fitb{}{@ifsoln{`lesson-folder`, a string which looks like some web address}} +
@fitb{}{}

@n What happens on lines 4-12? @fitb{}{@ifsoln{A bunch of images are defined, and it looks like they all come from that address}} +
@fitb{}{}

@vspace{1ex}

*Pyret has a number of functions that compute numbers, describing different features of an image:*

[cols="1a,1a", frame="none"]
|===
|
@show{(contract "image-width" '(Image) "Number")}
@show{(contract "image-height" '(Image) "Number")}
@show{(contract "image-luminance" '(Image) "Number")}

|
@show{(contract "image-symmetry-horizontal" '(Image) "Number")}
@show{(contract "image-entropy" '(Image) "Number")}
@show{(contract "image-symmetry-vertical" '(Image) "Number")}

|===

@n Experiment with them in the Interactions Area (for example, `image-luminance(img9)`).

@n What do you think `image-luminance` tells us about an image? @fitb{}{@ifsoln{the average brightness}} +
@fitb{}{}

@star What do you think `image-entropy` tells us about an image? @fitb{}{@ifsoln{How non-uniform it is}} +
@fitb{}{}


== Adding Attributes and Spaces
*`decorated` is a table of smaller images, with attributes for all the functions you just used (and more!)*

@n In the Interactions Area, evaluate `decorated`, `sun1`, and then `doc-thumbnail(sun1)`. Explain what you get for each one. @fitb{}{} +
@blanklines{2}{
@ifsoln{`sun1` is a row from `decorated`, and `doc-thumbnail(sun1)` is a tiny version of the image in `sun1`}
}

@n Make a scatter plot of "Luminance-Entropy Space", using the `decorated` table, `luminance` for the xs and `entropy` for the ys.

@n In the Interactions Area, evaluate `image-scatter-plot(decorated, "luminance", "entropy", doc-thumbnail)`. What happens? @fitb{}{} +
@fitb{}{@ifsoln{We can see the image, plotted in Luminance-Entropy Space!}}

@n Do you see any _clusters_, where similar images are grouped close together? @fitb{}{} +
@blanklines{2}{
@ifsoln{The bottom-right (low-entropy, high-luminance) has images of snowy mountains with very little else. The high-contrast images are close together in the top-left (high-entropy, low-luminance)}
}

@n Try making other `image-scatter-plot`s using the other axes. Are there any image spaces that give you even better clustering?

@n What is the name of the last column? @fitb{8em}{@ifsoln{`color-names`}} @hspace{1em} What do you think it's telling us about the image? @fitb{}{} +
@blanklines{1}{@ifsoln{the dominant color for each pixel}}