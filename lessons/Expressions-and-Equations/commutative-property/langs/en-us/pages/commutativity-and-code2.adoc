= Does Order Matter in the Code? (Continued)

++++
<style>
#preamble_disabled { flex-grow: 0 !important; }
</style>
++++

[.no-flex-section]
---
This page is designed for use with the @starter-file{expressions-equations}. You will be investigating four functions:

[.contracts, cols="1,1", frame="none", grid="none"]
|===
| @show{ (contracts
'("string-contains" ("String" "String") "Boolean")
'("blend-images" ("Image" "Image") "Image")
)}
| @show{ (contracts
'("darker" ("Image" "Image") "Image")
'("lighter" ("Image" "Image") "Image")
)}
|===

For each function, write two expressions such that the order of the inputs changes. Take a moment to predict what @proglang will return for each. Then test your code and record the result.

---

== Does the order of the arguments matter for @show{(code 'string-contains)}?

[.FillVerticalSpace, cols="^.^1,^.^4,^.^4"]
|===
| Code
| @show{(code '(string-contains "rainbow" "bow"))}
| @ifsoln{@show{(code '(string-contains "bow" "rainbow"))}}

| Result
| `true`
| @ifsoln{`false`}

|===

@n What does `string-contains` do? @fitb{}{@ifsoln{checks to see whether the first string contains the second string}}

@n Did both expressions produce _identical_ results? @fitb{7em}{@ifsoln{No.}} @hspace{2em} 

== Does the order of the arguments matter for @show{(code 'blend-images)}?

Start by testing out `purple-square` and `white-dot` in the Interactions Area.

[.FillVerticalSpace, cols="^.^1,^.^4,^.^4"]
|===
| Code
| @show{(code '(blend-images purple-square white-dot))}
| @ifsoln{@show{(code  '(blend-images white-dot purple-square))}}

| Sketch
| @image{../images/blended.png, 60}
| @ifsoln{@image{../images/blended.png, 60}}
|===

@n What does `blend-images` do? @fitb{}{@ifsoln{blends the two images together, averaging the pixels that overlap}}

@n Did both expressions produce _identical_ images? @fitb{7em}{@ifsoln{Yes.}} 

== Does the order of the arguments matter for @show{(code 'darker)} and @show{(code 'lighter)}?

Start by testing out `imageA` and `imageB` in the Interactions Area.

[.FillVerticalSpace, cols="^.^1,^.^4,^.^4"]
|===
| Code
| @show{(code '(darker imageA imageB))}
| @showsoln{(code '(darker imageB imageA))}

| Result
| @image{../images/darker-a-b.png, 50%}
| @ifsoln{@image{../images/darker-a-b.png, 50%}}

|===

@n Did both expressions produce the same result? @fitb{7em}{@ifsoln{Yes.}} 

@n Test `cat` and `dog` in the Interactions Area. Then, before testing @show{(code '(darker cat dog))}, imagine what it will produce.

@n What does `darker` do? @fitb{}{combine two images into one such that each pixel of the new image comes from}
@fitb{}{@ifsoln{looking at the two pixels in that position and choosing the darker.}}

@n Make a prediction. What do you think `lighter` does? @fitb{}{}

@n Do you think order matters for @show{(code 'lighter)}?  @fitb{8em}{} @hspace{2em} Test your prediction. Were you right? @fitb{}{}

