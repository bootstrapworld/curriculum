= Which One Doesn't Belong?

Identify the Circle of Evaluation that does not belong with the others.

++++
<style>
.chosen::after { content: '❌' !important; }

/* for table cells with immediate .content children, which have immediate
 * .paragraph children: use flex to space them evenly and center vertically
*/
td > .content > .paragraph:has(.circleevalsexp) {
  display: flex;
  align-items: center;
  justify-content: space-around;

/* Make autonums inside tables look consistent with those outside */
table .autonum::after { content: ')' !important;}

}
</style>
++++

[.FillVerticalSpace, cols="^.^1a,^.^8a, ^.^3a",stripes="none", options="header"]
|===
| 	 | Circles of Evaluation | Justify your Choice

| @n
| 
[cols="^1a,^1a,^1a,^1a", grid="none", frame="none"]
!===
!@show{(coe '(+ 20 -20))}
!@show{(coe '(+ -20 20))}
!@show{(coe '(- 20 20))}
!@ifsoln-choice{@show{(coe '(- -20 20))}}
!===
| @ifsoln{The fourth expression has 2 `-`, whereas the others have one `-`.}

| @n
| 
[cols="^1a,^1a,^1a,^1a", grid="none", frame="none"]
!===
!@show{(coe '(+ -2 5))}
!@show{(coe '(+ 5 -2))}
!@show{(coe '(- 5 2))}
!@ifsoln-choice{@show{(coe '(- 2 5))}}
!===
| @ifsoln{The fourth expression applies the +
`-` to the 5 instead of the 2.}

| @n
| 
[cols="^1a,^1a,^1a,^1a", grid="none", frame="none"]
!===
!@show{(coe '(+ 6 -3))}
!@show{(coe '(- 6 3))}
!@ifsoln-choice{@show{(coe '(- 3 6))}}
!@show{(coe '(+ -3 6))}
!===
| @ifsoln{The fourth expression applies the +
`-` to the 6 instead of the 3.}

| @n
| 
[cols="^1a,^1a,^1a,^1a", grid="none", frame="none"]
!===
!@show{(coe '(+ 100 -30))}
!@ifsoln-choice{@show{(coe '(- -30 100))}}
!@show{(coe '(+ -30 100))}
!@show{(coe '(- 100 30))}
!===
| @ifsoln{The second expression has 2 `-`, whereas the others have one `-`.}

| @n
| 
[cols="^1a,^1a,^1a,^1a", grid="none", frame="none"]
!===
!@ifsoln-choice{@show{(coe '(- 50 0))}}
!@show{(coe '(+ 0 -50))}
!@show{(coe '(+ -50 0))}
!@show{(coe '(- 0 50))}
!===
| @ifsoln{The first expression applies the +
`-` to the 0 instead of the 50.}

| @n
| 
[cols="^1a,^1a,^1a,^1a", grid="none", frame="none"]
!===
!@show{(coe '(- 5 8))}
!@show{(coe '(+ 5 -8))}
!@show{(coe '(+ -8 5))}
!@ifsoln-choice{@show{(coe '(- 8 5))}}
!===
| @ifsoln{The fourth expression applies the +
`-` to the 5 instead of the 8.}
|===

