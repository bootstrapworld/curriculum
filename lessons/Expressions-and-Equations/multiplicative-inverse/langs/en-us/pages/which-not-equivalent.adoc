= Are They Equivalent? Multiplicative Inverse

For each row, identify all of the Circle(s) of Evaluation that evaluate to the provided quantity. In some cases, it might be all of the expressions.

++++
<style>
.circleevalsexp { width: auto; }

/* for table cells with immediate .content children, which have immediate
 * .paragraph children: use flex to space them evenly and center vertically
*/
td > .content > .paragraph {
  display: flex;
  align-items: center;
  justify-content: space-around;
}
/* Allow the parent (.paragraph) to control the display of the child elements */
td > .content > .paragraph > p { display: contents; }
</style>
++++

[.FillVerticalSpace, cols="<.^3a,^.^11a",stripes="none", options="header"]
|===
| 
| Circles of Evaluation

| @n Which evaluate to @math{10}?
| 
@ifsoln-choice{@show{(coe '(* 30 1/3))}}
@ifsoln-choice{@show{(coe '(* 1/3 30))}}
@ifsoln-choice{@show{(coe '(/ 30 3))}}
@show{(coe '(/ 1/3 30))}

| @n Which evaluate to @math{6}?
| 
@show{(coe '(/ 24 1/4))}
@ifsoln-choice{@show{(coe '(* 24 1/4))}}
@show{(coe '(* 1/4 24))}
@ifsoln-choice{@show{(coe '(/ 24 4))}}

| @n Which evaluate to @math{\frac{2}{3}}?
| @show{(coe '(/ 3 2))}
@ifsoln-choice{@show{(coe '(/ 2 3))}}
@ifsoln-choice{@show{(coe '(* 2 1/3))}}
@ifsoln-choice{@show{(coe '(* 1/3 2))}}

| @n Which evaluate to @math{\frac{4}{5}}?
| 
@ifsoln-choice{@show{(coe '(/ 4 5))}}
@ifsoln-choice{@show{(coe '(* 4 1/5))}}
@ifsoln-choice{@show{(coe '(* (* 2 2) 1/5))}}
@show{(coe '(/ 5 4))}

| @n Which evaluate to @math{\frac{10}{12}}?
| 
@ifsoln-choice{@show{(coe '(* 10 1/12))}}
@ifsoln-choice{@show{(coe '(/ 10 12))}}
@ifsoln-choice{@show{(coe '(* 2 (* 1/12 5)))}}
@ifsoln-choice{@show{(coe '(* (* 2 5) 1/12))}}

| @n Which evaluate to @math{\frac{8}{9}}?
| 
@ifsoln-choice{@show{(coe '(* (* 2 4) 1/9))}}
@ifsoln-choice{@show{(coe '(/ 8 9))}}
@ifsoln-choice{@show{(coe '(/ (* 4 2) 9))}}
@ifsoln-choice{@show{(coe '(* 8 1/9))}}
|===

