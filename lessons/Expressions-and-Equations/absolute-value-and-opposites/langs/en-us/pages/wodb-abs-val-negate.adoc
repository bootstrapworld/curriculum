= Which One Doesn't Belong? Absolute Value & Negate

++++
<style>
.chosen::after { content: '❌' !important; }
.circleevalsexp { width: auto; }

/* for table cells with immediate .content children, which have immediate
 * .paragraph children: use flex to space them evenly and center vertically
*/
td > .content > .paragraph {
  display: flex;
  align-items: center;
  justify-content: space-around;
}
/* Allow the parent (.paragraph) to control the display of the child elements */
td > .content > .paragraph > p { display: contents; }
</style>
++++

Identify the circle of evaluation in each row below that doesn't evaluate to the same things as the others.

[.FillVerticalSpace, cols="<.^1a,^.^10a",stripes="none"]
|===
| @n
|
@ifsoln-choice{@show{(coe '(negate 6))}}
@show{(coe '(negate -6))}
@show{(coe '(abs 6))}
@show{(coe '(abs -6))}

| @n
|
@show{(coe '(negate -4))}
@ifsoln-choice{@show{(coe '(negate 4))}}
@show{(coe '(abs 4))}
@show{(coe '(abs -4))}

| @n
|
@show{(coe '(abs 3))}
@show{(coe '(abs -3))}
@ifsoln-choice{@show{(coe '(negate (negate -3)))}}
@show{(coe '(negate -3))}

| @n
|
@show{(coe '(abs (negate m)))}
@ifsoln-choice{@show{(coe '(negate (negate -m)))}}
@show{(coe '(abs m))}
@show{(coe '(abs -m))}

| @n
|
@ifsoln-choice{@show{(coe '(negate (abs h)))}}
@show{(coe '(negate -h))}
@show{(coe '(abs h))}
@show{(coe '(negate(negate h)))}

| @n
|
@show{(coe '(negate (abs x)))}
@show{(coe '(negate (abs -x)))}
@ifsoln-choice{@show{(coe '(abs (negate x)))}}
@show{(coe '(negate (negate -x)))}
|===

