= Converting Between Angles

== Connecting Radians and Degrees
@n In the circle below, fill in the blanks to label the number of degrees and radians at each point.

@ifnotsoln{@center{@image{../images/convert-circle.png, 400}}}
@ifsoln{@center{@image{../images/convert-circle-soln.png, 400}}}

@define{leg}{}

== Computing *cosine* and *sine*

Use `cos`, `sin` and `sin-deg` in the @starter-file{alg2-unit-clock} to complete the table. _Check whether each function consumes radians or degrees!_

[.FillVerticalSpace, cols="^.^1a, ^.^2a, ^.^2a, ^.^2a", options="header"]
|===
| Angle (θ)	

| 	`cos(θ)` + 
_consumes radians_			
| 	`sin(θ)` +
_consumes radians_			
|	`sin-deg(θ)` +
_consumes degrees_	

|	0° / @math{0  \pi}		| `cos(0) = 1`					| 0						| @ifsoln{0}				
|  45° / @math{0.25\pi}		| `cos(0.25 * PI)` = ~ 0.7071	| ~ 0.7071				| @ifsoln{~ 0.7071}
|  90° / @math{0.50\pi}		|  0							| 1 					| @ifsoln{1}
| 135° / @math{0.75\pi}		| ~ - 0.7071					| @ifsoln{~ 0.7071}		| ~ 0.7071	
| 180° / @math{1.00\pi}		| -1 							| @ifsoln{0}			| 0
| 225° / @math{1.25\pi}		| ~ - 0.7071					| @ifsoln{~ - 0.7071}	| ~ - 0.7071
| 270° / @math{1.50\pi}		| @ifsoln{0}					| -1 					| -1
| 315° / @math{1.75\pi}		| @ifsoln{~ 0.7071}				| ~ -0.7071				| ~ -0.7071
| 360° / @math{2 \pi}		| @ifsoln{1}					| 0						| 0
|===

@n Which function computes the *horizontal* leg of the right triangle (@math{A})? @fitb{}{@ifsoln{`cos`}}

@n Which function computes the *vertical* leg of the right triangle (@math{B})? @fitb{}{@ifsoln{`sin`}}

@n What is the relationship between the values in the `sin(0)` and `sin-deg(0)` columns and how might you explain it? @fitb{}{}

@blanklines{1}{@ifsoln{They're exactly the same, because the radian angle measures we gave to `sin` were equivalent to the degree angle measures we gave to `sin-deg`}}

