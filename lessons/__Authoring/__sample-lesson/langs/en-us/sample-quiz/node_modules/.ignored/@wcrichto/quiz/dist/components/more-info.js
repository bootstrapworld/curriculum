import React, { useState } from "react";
import { MarkdownView } from "./markdown";
// TODO: replace this, the bug reporter, and the "why fullscreen?" text with popperjs
export let MoreInfo = ({ markdown }) => {
    let [open, setOpen] = useState(false);
    return (React.createElement("span", { className: "info-wrapper" },
        open && (React.createElement("div", { className: "info-popout" },
            React.createElement(MarkdownView, { markdown: markdown }))),
        React.createElement("span", { className: "info", onClick: () => setOpen(!open) })));
};
//# sourceMappingURL=more-info.js.map