import React from "react";
import type { Quiz } from "../bindings/Quiz";
import { type TaggedAnswer } from "../questions/mod";
declare global {
    var telemetry: {
        log: (endpoint: string, payload: any) => void;
    } | undefined;
}
export declare let useCaptureMdbookShortcuts: (capture: boolean) => void;
export interface QuizViewConfig {
    name: string;
    quiz: Quiz;
    fullscreen?: boolean;
    cacheAnswers?: boolean;
    allowRetry?: boolean;
    showBugReporter?: boolean;
    autoStart?: boolean;
}
export type QuizViewProps = QuizViewConfig & {
    onFinish?: (answers: TaggedAnswer[]) => void;
};
export declare let QuizConfigContext: React.Context<QuizViewConfig>;
export declare let generateQuestionTitles: (quiz: Quiz) => string[];
export declare let QuizView: React.FC<QuizViewProps>;
