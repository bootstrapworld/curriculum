export interface GenerateAssetsOptions {
    outDir: string;
    serverUrl: string;
}
export declare let generateAssets: ({ outDir, serverUrl }: GenerateAssetsOptions) => void;
export declare let rustEditorVitePlugin: ({ serverUrl }: {
    serverUrl: string;
}) => {
    writeBundle(options: any): void;
}[];
