import React from "react";
import { type SnippetOptions } from "./snippet";
declare global {
    function initAquascopeBlocks(root: HTMLElement): void;
}
export declare let MarkdownView: React.FC<{
    markdown: string;
    snippetOptions?: Partial<SnippetOptions>;
}>;
