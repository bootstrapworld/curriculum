; OK isn't a fan of common core, so let's hide those standards

; we're no longer using the Racket file that defines disallow-standards, since
; all alignment processing is now done in JS. maybe we need to get this functionality
; back by some other means
;
;(disallow-standards "CC-ELA" "CC-Math")
